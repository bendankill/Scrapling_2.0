# Scrapling eMAG Crawler V2.0 MVP
# 固定已验证通过的具体版本

scrapling[all]==0.4.12
curl_cffi>=0.13.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
openpyxl>=3.1.0
httpx>=0.27.0

# 以下为 scrapling 依赖，显式固定避免兼容性问题
browserforge>=1.1.0
patchright>=1.49.0
