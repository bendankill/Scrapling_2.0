# Scrapling eMAG Crawler V2.0.2
# Pure HTTP mode: FetcherSession only, no browser launched
scrapling[fetchers]==0.4.12
beautifulsoup4==4.12.3
openpyxl==3.1.5
httpx==0.27.2
pytest==8.3.4
# Note: scrapling[fetchers] transitively installs curl_cffi, playwright (Python
# package for error types only), and browserforge (header generation).
# patchright is uninstalled post-install by 01_install.bat.
# No Chromium or other browser binary is downloaded or launched.
