"""
项目统一默认配置 V2.1.3
============================================================
这里是并发默认值的唯一修改位置。
如果命令行显式传入 --page-workers、--category-workers 或 --max-in-flight，
命令行参数会覆盖这里的默认值。
不要只修改Crawler构造函数默认值，因为调用方显式传值时构造函数默认值不会生效。
============================================================
"""
# 页面并发：同一类目内同时抓取的最大页面数
DEFAULT_PAGE_WORKERS = 1
# 类目并发：同时抓取的最大类目数
DEFAULT_CATEGORY_WORKERS = 1
# 全局最大并发请求数：页面请求和图片下载共享的Semaphore上限
DEFAULT_MAX_IN_FLIGHT = 4
# 图片下载并发工作线程数
DEFAULT_IMAGE_WORKERS = 8
