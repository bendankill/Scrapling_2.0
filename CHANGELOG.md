# 更新日志

## V2.0.3 (2026-08-03)

### 修复
- **并发页面乱序不再丢数据**: 采用按页码顺序缓冲提交机制，页面并发获取但严格按页码顺序处理和写入
- **--all-pages 固定为20页**: 每个类目最多抓取20页，不再使用伪无限值
- **真正Session复用**: 使用`threading.local()`+`__enter__()`实现每线程底层HTTP客户端复用（Cookie/TCP/TLS）
- **图片错误写入 errors.csv**: 图片下载失败详情（URL、错误类型、HTTP状态码、商品键）统一写入 errors.csv
- **测试服务器动态端口**: 使用端口0+`ThreadingHTTPServer`，支持并发测试和重复运行
- **并发乱序测试**: 6个场景验证按序提交、数据不丢失、重复页正确判断
- **Session复用测试**: 同线程同客户端、不同线程隔离、Cookie连续性、安全关闭
- **图片错误测试**: 404、超时、HTML响应、过小图片、不识别格式、重定向、同URL多商品

### 技术要点
- Scrapling `FetcherSession.__enter__()` 返回 `_SyncSessionLogic`，可复用`get()`方法
- 每线程一次`__enter__()`，后续请求直接调用`client.get(url)`
- 不同线程拥有独立底层HTTP客户端

## V2.0.2 (2026-08-03)
- 依赖冲突修复、WAF检测增强、分页重构、跨类目去重隔离

## V2.0.1 (2026-08-03)
- 纯HTTP模式、TXT配置、JSON输出

## V2.0.0 MVP (2026-08-03)
- 初始版本
