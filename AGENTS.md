# Scrapling 2.0 项目规则

## 项目目标

本项目用于抓取 eMAG 分类页面商品信息，优先保证数据准确性、运行效率和异常处理完整性。

## 开发范围

- 修改前先阅读项目现有代码、测试和配置。
- 保持现有命令行参数与输出格式兼容。
- 不恢复已经移除的断点续抓功能。
- 不擅自增加浏览器自动化方案，继续使用项目现有 HTTP 抓取架构。
- 不修改与当前需求无关的抓取、导出和图片下载逻辑。

## 验证要求

- 修改后运行现有单元测试和集成测试。
- 检查正常完成、网络错误、WAF、Ctrl+C 中断和导出流程。
- 涉及统计数据时，验证控制台数量、JSON、CSV、XLSX结果一致。
- 如需真实网站测试，先使用一个类目和一页进行最小测试。

## Git 要求

- 开发前确认当前分支。
- 未经用户明确要求，不推送、不合并到 main、不创建标签。
- 不修改用户工作区内与当前任务无关的内容。
- 用户要求提交新版本时，必须同步更新 README 首页标题、版本记录和 CHANGELOG；提交前确认 GitHub 默认分支主页已经包含该版本记录。
